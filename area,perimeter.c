#include<stdio.h>
void main()
{
	int s,area,per;
	s = 2;
	area = s*s;
	per = 4*s;
	printf("area of the square with side %d is %d\nperimeter of the square with side %d is %d",s,area,s,per);
}
