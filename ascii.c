#include<stdio.h>
void main()
{
	int a,b,c;
	printf("Enter a character (a-z or A-Z):");
	scanf("%d",&a);
	printf("Corresponding ASCII character is %c is %d",a,a);
	printf("Enter a character (a-z or A-Z):");
    scanf("%d",&b);
	printf("Corresponding ASCII character is %d is %c",b,b);
	
	
	
	
	
}
